const fs=require('fs'),vm=require('vm'),crypto=require('crypto');const src=fs.readFileSync('app.js','utf8');
function extract(name){let a=src.indexOf('function '+name+'(');if(a<0)return'';let p=src.indexOf('(',a),pd=0,q=null,e=false,b=-1;for(let i=p;i<src.length;i++){let c=src[i];if(q){if(e)e=false;else if(c==='\\')e=true;else if(c===q)q=null;continue}if(c==='"'||c==="'"||c==='`'){q=c;continue}if(c==='(')pd++;else if(c===')'&&--pd===0){b=src.indexOf('{',i+1);break}}let d=0;q=null;e=false;for(let i=b;i<src.length;i++){let c=src[i];if(q){if(e)e=false;else if(c==='\\')e=true;else if(c===q)q=null;continue}if(c==='"'||c==="'"||c==='`'){q=c;continue}if(c==='{')d++;else if(c==='}'&&--d===0)return src.slice(a,i+1)}return''}
const ctx={Blob,TextEncoder,console};vm.createContext(ctx);let hs=src.slice(src.indexOf('function crc32V47'),src.indexOf('function downloadBlobV47'));vm.runInContext(hs,ctx);
(async()=>{const names=['data_snapshot.json','markets.csv','fiscal.csv','macro.csv','correlations.csv','commodities.csv','early_warning.csv','global_risk.csv','metadata.json','README.txt'];const z=ctx.zipStoreV47(names.map((name,i)=>({name,data:i===9?'Hrvatska čćžšđ':'{"ok":true}'})));const raw=Buffer.from(await z.arrayBuffer());const sha=crypto.createHash('sha256').update(raw).digest('hex');const b64=raw.toString('base64');const dec=Buffer.from(b64,'base64');const sha2=crypto.createHash('sha256').update(dec).digest('hex');fs.writeFileSync('/mnt/data/mrm_v35148/native_export_roundtrip_v48.zip',dec);if(raw.readUInt32LE(0)!==0x04034b50||sha!==sha2)process.exit(1);console.log('PASS ZIP PK signature');console.log('PASS SHA256 roundtrip '+sha);console.log('PASS UTF8 fixture');})();
