# v3.6.1 build 110 — Compact stock/ETF alarm badge hotfix

Scope is intentionally limited to presentation.

- Reduced font size, line height, padding, gap and top margin of the volatility/downside alarm badge **only inside `.stock-row`**, which covers stock and ETF list rows.
- Alarm label, status, colors, title/tooltip, calculations and source data are unchanged.
- Detailed alarm explanations and the dedicated Volatility tab retain the original size.
- `app.js` is unchanged from v3.6.1 build 109.
- versionName remains `3.6.1`; Android versionCode is incremented to `110` so the APK can upgrade the previous build.
