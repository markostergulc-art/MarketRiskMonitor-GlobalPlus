# Release signing material

This source bundle intentionally includes the project's existing release signing files at the owner's request:

- `signing/MarketRiskMonitor_GlobalPlus_release.p12`
- `signing/keystore.properties`

Treat the complete source ZIP as confidential. Do not publish it to a public repository or share it with untrusted parties. Do not regenerate the signing identity for normal upgrades.
